#include<bits/stdc++.h>
using namespace std;

int main() {
    int p, q; cin >> p >> q;
    int len = q-p+1;

    for(int i = 0; i < (1 << len); i++) {
        for(int j = 0; j < len; j++) {
            if((i >> j)&1) {
        }
    }


    return 0;
}
